import { it, expect, describe } from 'vitest'

// This test is just a test to test the testing :)
// #TODO: Delete later
describe('group', () => {
  it('should', () => {
    expect(1).toBeTruthy()
  })
})